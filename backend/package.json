{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun index.ts",
    "dev:auth": "ALLOW_AUTH_OVERRIDE=false bun dev",
    "tsc": "! tsc --noEmit 2>&1 | grep -v 'node_modules/.bun/bun-sqlite-migrations' | grep 'error TS'"
  },
  "dependencies": {
    "cookie": "1.0.2",
    "supertokens-node": "catalog:",
    "jsonwebtoken": "9.0.2",
    "jwks-rsa": "3.2.0",
    "bun-sqlite-migrations": "1.0.2",
    "zod": "catalog:",
    "shared": "workspace:*",
    "cheerio": "^1.0.0",
    "ky": "catalog:"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "9.0.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
