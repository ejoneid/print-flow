### Start supertokens-core with docker
```bash
docker run \
    -p 3567:3567 \
    -e API_KEYS="development-supertokens-api-key" \
    -d registry.supertokens.io/supertokens/supertokens-postgresql
```
